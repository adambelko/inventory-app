
<%- include("./partials/head") -%>

<body>

	<%- include("./partials/sidebar") -%>

	<main>
		<h2><%= title %></h2>
        <% console.log(item_instance_list) %>
		<ul>
			<% item_instance_list.map((itemInstance) => { %>
				<li><%= itemInstance.item.name %> - $<%= itemInstance.item.price %></li>
			<% }) %>
		</ul>
	</main>
</body>
</html>
